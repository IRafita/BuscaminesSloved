#include <stdlib.h>
#include "estructures.h"

int
main (int argc, char **argv)
{
	int mines;
	struct relativePointer null;
	struct srp explorer, fasth, raonar, sleep;

/* molts parsers per a entrar les dades... */
	if (argc != 2) /* Canviara */ exit (1);

/* un init decent */
	null.rp = 120;




	mines = 170;

	while (mines)
	{
		mines--;
		if (explorer.begin.rp != null.rp)
		{
		}
		else if (fasth.begin.rp != null.rp)
		{
		}
		else if (raonar.begin.rp != null.rp)
		{
		}
		else
			exit (3);
	}
return 0;
}
